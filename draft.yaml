models:
  GalleryPost:
    image_name: string
    created_by_id: unsignedBigInteger foreign:users.id
  BlogPost:
    title: string
    description: text
    image_name: string nullable
    created_by_id: unsignedBigInteger foreign:users.id
